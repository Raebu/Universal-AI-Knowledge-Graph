# UI Placeholders

This repository currently provides an API and connector framework. It does not yet include a dedicated user interface.

When a UI is added, include images for:

- Search dashboard
- Source connector setup
- Entity graph explorer
- Audit log view
- Workspace settings

For now, use the built-in FastAPI documentation routes during local development.
